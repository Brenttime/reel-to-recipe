[pytest]
markers =
    web: Tests that require the web container (port 5101)
    mcp: Tests that require the MCP server (port 8001/8002)
    slow: Tests that may take longer (network calls, conversions)
testpaths = tests
